const Loading = () => {
    return (
        <div className="text-center h-screen flex justify-center items-center bg-slate-800 text-white">
            <h1 className="text-4xl">Loading....</h1>
        </div>
    );
}

export default Loading;
